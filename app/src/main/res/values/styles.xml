<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="MyAppGrayText">
        <item name="android:textColor">@color/divider</item>
    </style>

    <style name="MyAppTitleText">
        <item name="android:textSize">18sp</item>
    </style>

    <style name="MyAppPriceyExpensive" parent="MyAppTitleText">
        <item name="android:textColor">@color/divider</item>
    </style>

    <style name="MyAppPriceyCheap" parent="MyAppTitleText">
        <item name="android:textColor">@color/soft_gray</item>
    </style>

    <style name="customRatingBar" parent="android:style/Widget.Material.RatingBar.Small">
        <item name="android:progressDrawable">@drawable/customratingstars</item>
        <item name="android:minHeight">25dip</item>
        <item name="android:maxHeight">25dip</item>
    </style>

</resources>
